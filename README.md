# HomeFinder

Real estate assistance portal built with PHP, MySQL, HTML, CSS, and JavaScript.

## Setup

1. Import `sql/homefinder.sql` into MySQL.
2. Place the folder in XAMPP `htdocs/homefinder`.
3. Update `php/db.php` if your credentials differ.
4. Open `http://localhost/homefinder/index.php`.

